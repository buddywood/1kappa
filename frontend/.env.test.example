# Playwright Test Environment Variables
# Copy this file to .env.local and fill in your test credentials

# Base URL for tests (default: https://preview.one-kappa.com)
PLAYWRIGHT_BASE_URL=https://preview.one-kappa.com

# =============================================================================
# SHARED PASSWORD FOR ALL TEST USERS
# =============================================================================
# All test users share the same password
TEST_PASSWORD=TestPassword123!

# =============================================================================
# TEST USER EMAILS BY ROLE
# =============================================================================
# Configure different email addresses for each user role
# Each role will use the TEST_PASSWORD defined above

# Guest user (basic access)
TEST_GUEST_EMAIL=test-guest@example.com

# Member user (standard member access)
TEST_MEMBER_EMAIL=test-member@example.com

# Steward user (steward privileges)
TEST_STEWARD_EMAIL=test-steward@example.com

# Seller user (seller/vendor access)
TEST_SELLER_EMAIL=test-seller@example.com

# Promoter user (promoter access)
TEST_PROMOTER_EMAIL=test-promoter@example.com

# Admin user (administrative access)
TEST_ADMIN_EMAIL=test-admin@example.com

# =============================================================================
# SPECIAL TEST ACCOUNTS
# =============================================================================
# Test user that needs email verification
TEST_UNVERIFIED_EMAIL=test-unverified@example.com

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# CI environment flag (automatically set in CI environments)
# CI=true

# =============================================================================
# EXAMPLE CONFIGURATION
# =============================================================================
# Here's an example of how your .env.local might look:
#
# PLAYWRIGHT_BASE_URL=https://preview.one-kappa.com
# TEST_PASSWORD=TestPassword123!
# TEST_GUEST_EMAIL=qa-guest@yourdomain.com
# TEST_MEMBER_EMAIL=qa-member@yourdomain.com
# TEST_STEWARD_EMAIL=qa-steward@yourdomain.com
# TEST_SELLER_EMAIL=qa-seller@yourdomain.com
# TEST_PROMOTER_EMAIL=qa-promoter@yourdomain.com
# TEST_ADMIN_EMAIL=qa-admin@yourdomain.com
