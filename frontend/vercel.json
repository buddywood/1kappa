{
  "regions": ["iad1"],
  "installCommand": "if [ -f package-lock.json ]; then npm ci --legacy-peer-deps --legacy-bundling; else npm install --legacy-peer-deps --legacy-bundling; fi",
  "buildCommand": "npm run build"
}
